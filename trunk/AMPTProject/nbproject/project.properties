application.homepage=http://ampt.sourceforge.net
application.title=AMPTProject
application.vendor=The AMPT Project
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
# Uncomment to specify the preferred debugger connection transport:
#debug.transport=dt_socket
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/AMPTProject.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.ampt-trunk-src=../src
file.reference.ampt-trunk-test=../test
file.reference.jMusic1.6.01.jar=../lib/jMusic1.6.01.jar
file.reference.AmptWinClock.dll=../lib/AmptWinClock.dll
includes=**
jar.compress=false
javac.classpath=\
    ${file.reference.jMusic1.6.01.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.6
javac.target=1.6
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=Additive MIDI Processing Tool (AMPT)
jnlp.codebase.type=local
jnlp.codebase.url=file:/H:/ampt-trunk/AMPTProject/dist/
jnlp.descriptor=application
jnlp.enabled=false
jnlp.offline-allowed=false
jnlp.signed=false
main.class=ampt.ui.MainWindow
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${file.reference.jMusic1.6.01.jar}:\
    ${file.reference.AmptWinClock.dll}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=-XX:+UseConcMarkSweepGC -Djava.library.path="..\\lib"
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=${file.reference.ampt-trunk-src}
test.src.dir=${file.reference.ampt-trunk-test}
